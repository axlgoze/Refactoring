const fs=require("fs")
class Reader{
	static readJsonFile(){ //debe recibir una path del archivo a leer
		//logica para leer archivo y regresar informacion
		//read json file
		const raw data = 
	}
}

module.exports = Reader //exportando clase
